services:
  redis:
    image: redis
    volumes:
      - ./redis_data:/data
    ports:
      - 6379:6379
  
  redisinsight:
    image: redis/redisinsight
    volumes:
      - ./redisinsight_data:/data
    ports:
      - 5540:5540

networks:
  huongdanjava:
    driver: bridge